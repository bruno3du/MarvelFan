/** @format */

import type { NextPage } from 'next';
import Head from 'next/head';
import BasicLayout from '../styles/basic';

const Home: NextPage = () => {
	return (
		<BasicLayout>
			<Head>
				<title>Marvel</title>
			</Head>
			
		</BasicLayout>
	);
};

export default Home;
